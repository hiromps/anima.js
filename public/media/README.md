# メディア素材について

このディレクトリのファイルは **anima.js のサイト上でプレビューを表示するためだけ**に置かれています。

リポジトリの [MIT ライセンス](../../LICENSE) が対象とするのはソースコードであり、**ここにあるメディアは対象外**です。再利用できる前提で扱わないでください。

コンポーネントを自分のプロジェクトで使う場合は、`items` にご自身のメディアを渡してください（省略するとプレースホルダーのグラデーションカードが表示されます）。
